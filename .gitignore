__pycache__/
*.pyc
*.pyo
.env
.venv
venv/
node_modules/
*.egg-info/
dist/
build/
.DS_Store
Thumbs.db
*.log
_refs/
.claude/
**/embeddings.json
bash.exe.stackdump
fix_nginx.py
OCC_DESIGN_SYSTEM.md
admin/

# Legacy service files — kept here so an unmigrated clone doesn't try to
# track them. After the auto-migration on first launch these live under
# ~/.occ/ instead, but leaving the entries protects users mid-upgrade.
.occ_config.json
.occ_chats.json
.occ_chats.json.migrated
deliberation_log.md
workspace/
upload/

# Expert pack content is user/community-built. The repo ships only the
# directory shell so Forge has somewhere to write. Real packs are deployed
# to the broker via admin/hub.py and discovered via /search.
expert-packs/*
!expert-packs/.gitkeep

# Per-session working drafts at the repo root (knowledge reports, scratch).
# Pack files with similar names are still tracked normally inside packs.
/occ-*.md

# Local planning docs — never pushed
docs/
